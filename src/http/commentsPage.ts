export const commentsPage = `<!doctype html>
<html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Stopirex — Bình luận</title><style>
:root{color-scheme:light;--ink:#162033;--muted:#637189;--line:#dfe5ee;--panel:#fff;--bg:#f5f7fb;--blue:#2459d3;--green:#138a5b;--amber:#b76a00;--red:#c73737;--purple:#7c3aed}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,Montserrat,Arial,sans-serif}main{max-width:1540px;margin:0 auto;padding:24px}.hero{padding:24px 26px;border-radius:20px;background:linear-gradient(135deg,#102b5e,#2459d3);color:#fff;box-shadow:0 12px 32px #193e6b20}.hero-row,.section-head,.toolbar{display:flex;justify-content:space-between;align-items:center;gap:16px}.hero h1{margin:0 0 7px;font-size:28px}.hero p{margin:0;opacity:.84}.refresh{border:1px solid #ffffff55;color:#fff;background:#ffffff18;border-radius:10px;padding:10px 14px;font-weight:800;cursor:pointer}.refresh:disabled{opacity:.55}.tabs{display:flex;gap:6px;margin:16px 0 18px;padding:5px;background:#e9edf4;border-radius:12px;width:max-content}.tab{padding:9px 14px;border-radius:9px;color:#4a5870;text-decoration:none;font-weight:700;font-size:14px}.tab.active{color:#19489f;background:#fff;box-shadow:0 2px 8px #17294a16}.grid{display:grid;gap:14px}.kpis{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:16px}.card,.panel{background:var(--panel);border:1px solid var(--line);box-shadow:0 8px 26px #132f5b0a}.card{border-radius:16px;padding:17px}.card label{display:block;color:var(--muted);font-size:11px;font-weight:800;margin-bottom:9px}.card strong{font-size:27px}.card small{display:block;color:var(--muted);margin-top:6px}.panel{border-radius:18px;padding:20px}.panel h2{font-size:18px;margin:0}.note{font-size:12px;color:var(--muted)}.toolbar{margin:14px 0}.filters{display:flex;gap:8px;flex-wrap:wrap}.filters input,.filters select{border:1px solid #cad3e0;background:#fff;border-radius:9px;padding:9px 11px;font:inherit;font-size:13px}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:12px}table{width:100%;border-collapse:collapse;min-width:1480px}th,td{text-align:left;padding:11px 12px;border-bottom:1px solid #e8ecf2;font-size:12px;vertical-align:top}th{position:sticky;top:0;background:#f7f9fc;color:#5b6980;font-size:11px;text-transform:uppercase;letter-spacing:.03em}tbody tr:hover{background:#f8faff}.pill{display:inline-flex;border-radius:999px;padding:5px 9px;font-size:10px;font-weight:800;white-space:nowrap}.replied,.normal,.keep{color:var(--green);background:#e9f7f1}.received,.processing,.partial,.review{color:var(--amber);background:#fff4df}.failed,.urgent,.hide{color:var(--red);background:#ffeded}.paused,.hidden{color:var(--purple);background:#f1eaff}.category{color:#2459d3;background:#eaf0ff}.message{max-width:300px;line-height:1.45;white-space:pre-line}.comment{font-weight:650}.sub{display:block;color:#7a8698;margin-top:4px;font-size:11px}.action{display:block;margin-top:8px;border:1px solid #c9d2df;background:#fff;border-radius:8px;padding:7px 9px;font-weight:800;font-size:11px;cursor:pointer}.action.danger{color:var(--red);border-color:#efbcbc}.action.restore{color:var(--purple);border-color:#d4c1f5}.action:disabled{opacity:.5;cursor:wait}.empty{padding:18px;color:var(--muted)}.error{display:none;margin-bottom:14px;padding:12px 14px;border-radius:11px;color:var(--red);background:#ffeded}.loading{opacity:.55;pointer-events:none}@media(max-width:900px){main{padding:14px}.kpis{grid-template-columns:repeat(2,1fr)}.tabs{width:100%;overflow:auto}.tab{white-space:nowrap}}@media(max-width:560px){.kpis{grid-template-columns:1fr}.hero-row{align-items:flex-start;flex-direction:column}}
</style></head><body><main>
<section class="hero"><div class="hero-row"><div><h1>Theo dõi bình luận</h1><p>Mỗi comment được trả lời công khai trước, sau đó gửi đúng một tin nhắn riêng cô đọng theo nhu cầu.</p></div><button id="refresh" class="refresh">Làm mới</button></div></section>
<nav class="tabs"><a class="tab" href="/operations">Tổng quan kết nối</a><a class="tab" href="/orders">Đơn hàng</a><a class="tab active" href="/comments">Bình luận</a><a class="tab" href="/product">Thông tin sản phẩm</a><a class="tab" href="/pages">Fanpage</a></nav>
<div id="error" class="error"></div><section id="content">
<div class="grid kpis"><div class="card"><label>TỔNG COMMENT</label><strong id="total">—</strong><small>Trong danh sách gần nhất</small></div><div class="card"><label>ĐANG XỬ LÝ</label><strong id="working">—</strong><small>Nhận mới / đang gửi</small></div><div class="card"><label>ĐÃ TRẢ LỜI ĐỦ</label><strong id="replied">—</strong><small>Public + một tin nhắn riêng</small></div><div class="card"><label>CẦN XEM XÉT</label><strong id="attention">—</strong><small>Khiếu nại, lỗi hoặc gợi ý kiểm duyệt</small></div></div>
<section class="panel"><div class="section-head"><div><h2>Luồng comment gần nhất</h2><div id="count" class="note">—</div></div><div class="note">Chỉ nhân viên quyết định ẩn; hệ thống không tự động che khiếu nại thật.</div></div><div class="toolbar"><div class="filters"><select id="status"><option value="all">Tất cả trạng thái</option><option value="working">Đang xử lý</option><option value="replied">Đã trả lời</option><option value="attention">Cần chú ý</option></select><select id="category"><option value="all">Tất cả nhu cầu</option><option value="price">Hỏi giá</option><option value="consultation">Tư vấn</option><option value="complaint">Khiếu nại</option><option value="positive">Tích cực</option><option value="other">Khác</option></select><select id="moderation"><option value="all">Tất cả kiểm duyệt</option><option value="hide">Gợi ý ẩn</option><option value="review">Cần xem xét</option><option value="hidden">Đã ẩn</option><option value="keep">Giữ hiển thị</option></select><input id="search" placeholder="Tìm nội dung, Page, intent…"></div></div><div class="table-wrap"><table><thead><tr><th>Trạng thái</th><th>Page / thời gian</th><th>Comment khách</th><th>Phân loại</th><th>Kiểm duyệt</th><th>Trả lời công khai</th><th>Tin nhắn riêng duy nhất</th><th>Vấn đề</th></tr></thead><tbody id="rows"></tbody></table></div></section>
</section></main><script>
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const names={received:'Đã nhận',processing:'Đang xử lý',partial:'Đã gửi một phần',replied:'Đã trả lời đủ',failed:'Lỗi gửi',paused:'Bot tạm dừng'},categories={price:'Hỏi giá',consultation:'Tư vấn',complaint:'Khiếu nại',positive:'Tích cực',other:'Khác'},moderationNames={keep:'Giữ hiển thị',review:'Cần xem xét',hide:'Gợi ý ẩn'};let comments=[];const time=v=>v?new Intl.DateTimeFormat('vi-VN',{dateStyle:'short',timeStyle:'medium'}).format(new Date(v)):'—';
function filtered(){const status=document.getElementById('status').value,category=document.getElementById('category').value,moderation=document.getElementById('moderation').value,q=document.getElementById('search').value.trim().toLowerCase();return comments.filter(item=>(category==='all'||item.category===category)&&(moderation==='all'||moderation==='hidden'&&item.isHidden||moderation!=='hidden'&&!item.isHidden&&item.moderationRecommendation===moderation)&&(status==='all'||status==='working'&&['received','processing','partial'].includes(item.status)||status==='replied'&&item.status==='replied'||status==='attention'&&(item.priority==='urgent'||item.moderationRecommendation!=='keep'||['failed','paused'].includes(item.status)))&&(!q||[item.pageName,item.commentText,item.intent,item.category,item.moderationReason,item.publicReplyText,item.privateReplyText].filter(Boolean).join(' ').toLowerCase().includes(q)))}
function render(){const rows=filtered(),working=comments.filter(i=>['received','processing','partial'].includes(i.status)).length,replied=comments.filter(i=>i.status==='replied').length,attention=comments.filter(i=>i.priority==='urgent'||i.moderationRecommendation!=='keep'||['failed','paused'].includes(i.status)).length;document.getElementById('total').textContent=comments.length;document.getElementById('working').textContent=working;document.getElementById('replied').textContent=replied;document.getElementById('attention').textContent=attention;document.getElementById('count').textContent='Hiển thị '+rows.length+'/'+comments.length+' comment';document.getElementById('rows').innerHTML=rows.length?rows.map(item=>'<tr><td><span class="pill '+esc(item.status)+'">'+esc(names[item.status]||item.status)+'</span>'+(item.priority==='urgent'?'<span class="sub"><span class="pill urgent">Gấp</span></span>':'')+'</td><td><b>'+esc(item.pageName)+'</b><span class="sub">'+esc(time(item.receivedAt))+'</span><span class="sub">Khách …'+esc(item.externalCustomerId.slice(-6))+'</span></td><td class="message comment">'+esc(item.commentText)+'</td><td><span class="pill category">'+esc(categories[item.category]||item.category)+'</span><span class="sub">'+esc(item.intent||'LLM chưa phân loại')+'</span></td><td><span class="pill '+(item.isHidden?'hidden':esc(item.moderationRecommendation))+'">'+(item.isHidden?'Đã ẩn':esc(moderationNames[item.moderationRecommendation]||item.moderationRecommendation))+'</span><span class="sub">'+esc(item.moderationReason||'Không có dấu hiệu cần ẩn')+'</span><button class="action '+(item.isHidden?'restore':'danger')+'" data-comment-action="'+esc(item.id)+'" data-next-hidden="'+(!item.isHidden)+'">'+(item.isHidden?'Hiện lại':'Ẩn comment')+'</button></td><td class="message">'+esc(item.publicReplyText||'Đang chờ soạn…')+'<span class="sub">'+(item.publicSentAt?'Đã gửi '+esc(time(item.publicSentAt)):'Chưa gửi')+'</span></td><td class="message">'+esc(item.privateReplyText||'Đang chờ soạn…')+'<span class="sub">'+(item.privateSentAt?'Đã gửi '+esc(time(item.privateSentAt)):'Chưa gửi')+'</span></td><td>'+esc(item.errorCode||'—')+'</td></tr>').join(''):'<tr><td colspan="8" class="empty">Chưa có comment phù hợp bộ lọc.</td></tr>'}
async function toggleVisibility(id,hidden){if(!confirm(hidden?'Ẩn comment này khỏi người xem công khai? Comment vẫn được lưu trong trang quản trị.':'Hiện lại comment này công khai?'))return;const button=document.querySelector('[data-comment-action="'+id+'"]'),error=document.getElementById('error');if(button)button.disabled=true;try{const response=await fetch('/api/meta/comments/'+encodeURIComponent(id)+'/visibility',{method:'POST',headers:{'content-type':'application/json',accept:'application/json'},body:JSON.stringify({hidden})});const payload=await response.json();if(!response.ok)throw new Error(payload.error||('HTTP '+response.status));await load();error.style.display='none'}catch(cause){error.textContent='Không đổi được trạng thái comment: '+cause.message;error.style.display='block';if(button)button.disabled=false}}
async function load(){const button=document.getElementById('refresh'),content=document.getElementById('content'),error=document.getElementById('error');button.disabled=true;content.classList.add('loading');try{const response=await fetch('/api/meta/comments?limit=200',{headers:{accept:'application/json'}});if(!response.ok)throw new Error('HTTP '+response.status);comments=await response.json();render();error.style.display='none'}catch(cause){error.textContent='Không tải được bình luận: '+cause.message;error.style.display='block'}finally{button.disabled=false;content.classList.remove('loading')}}
document.getElementById('refresh').addEventListener('click',load);document.getElementById('status').addEventListener('change',render);document.getElementById('category').addEventListener('change',render);document.getElementById('moderation').addEventListener('change',render);document.getElementById('search').addEventListener('input',render);document.getElementById('rows').addEventListener('click',event=>{const button=event.target.closest('[data-comment-action]');if(button)void toggleVisibility(button.dataset.commentAction,button.dataset.nextHidden==='true')});load();setInterval(load,10000);
</script></body></html>`;
